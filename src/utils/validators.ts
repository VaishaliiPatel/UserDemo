export const isNull = (input: any) => {
    if (input === null || input === undefined || input === '') {
      return true;
    }
  }
  
  export const RegEx = {
    mobile: /[^0-9]/,
  }
export const IMAGE_URL = 'https://valeksalifecare.com/members/dist/img/user2-160x160.jpg';
